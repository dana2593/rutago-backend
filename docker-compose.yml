services:
  rutago-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rutago-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_ENV: development
    restart: unless-stopped
